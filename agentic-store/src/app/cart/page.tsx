import { CartPage } from "@/components/cart-page";

export const metadata = {
  title: "Your Cart | Nova Market",
  description: "Review the items in your Nova Market cart before checkout.",
};

export default function Page() {
  return <CartPage />;
}
